node_modules
.next
.env*
!.env.example
.DS_Store
*.tsbuildinfo
next-env.d.ts

.vercel
.gstack/
.qa/
.claude/

# Private verification reports (raw docx + sidecar meta).
# 생성된 익명화 출력은 별도로 커밋됨:
#   - skills/{slug}/examples/{report_id}.md
#   - public/sample-data/{slug}--{report_id}.json
#   - data/verification-reports.json (인덱스)
/verification-reports/

# 로컬 개발용 KV/Blob 폴백 저장소 (lib/store.ts) — 실배포에서는 Vercel KV/Blob을 씀
/.data/
